<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
      http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
   http://www.springframework.org/schema/security 
   http://www.springframework.org/schema/security/spring-security-3.1.xsd">

    <bean id="rememberMeServices" class="org.springframework.security.web.authentication.rememberme.TokenBasedRememberMeServices">
        <property name="alwaysRemember" value="true"/>
        <property name="parameter" value="j_remember_me"/>
        <property name="tokenValiditySeconds" value="151221"/>
        <property name="cookieName" value="tt"/>
        <property name="userDetailsService" ref="httpUserService"/>
        <property name="key" value="theTreeTracker"/>
    </bean>

    <security:global-method-security secured-annotations="enabled"/>
    <!-- This is where we configure Spring-Security  -->
    <security:http auto-config="true" use-expressions="true" access-denied-page="/Denied">
        <security:intercept-url pattern="/Login" access="permitAll"/>
        <security:intercept-url pattern="/Register" access="permitAll"/>
        <security:intercept-url pattern="/login.tt" access="permitAll"/>

        <security:intercept-url pattern="/images/**" access="permitAll"/>
        <security:intercept-url pattern="/js/**" access="permitAll"/>
        <security:intercept-url pattern="/css/**" access="permitAll"/>

        <security:intercept-url pattern="/**" access="isAuthenticated()"/>

        <security:form-login
                login-processing-url="/check_login"
                login-page="/Login"
                authentication-failure-url="/Login?error=true"
                default-target-url="/Dashboard"/>

        <security:logout
                invalidate-session="true"
                logout-success-url="/Login"
                logout-url="/Logout"/>

        <security:remember-me key="theTreeTracker" user-service-ref="httpUserService"/>
    </security:http>

    <!-- Declare an authentication-manager to use a custom userDetailsService -->
    <security:authentication-manager>
        <security:authentication-provider user-service-ref="httpUserService">
            <security:password-encoder ref="passwordEncoder"/>
        </security:authentication-provider>
    </security:authentication-manager>

    <!-- Use a Md5 encoder since the user's passwords are stored as Md5 in the database -->
    <bean class="org.springframework.security.authentication.encoding.Md5PasswordEncoder" id="passwordEncoder"/>
</beans>